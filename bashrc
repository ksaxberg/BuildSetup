
# ~/.bashrc: Adding customized features

# So you wanted to join a session if it exists, or make a new one if it doesnt?
#  just use $(tma test)
alias tma="tmux new-session -As"
alias tml="tmux list-sessions"
